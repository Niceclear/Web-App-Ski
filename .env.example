# =============================================================================
# ENVIRONNEMENT - Next.js Ski App
# =============================================================================
# Ce fichier sert de template. Copiez-le vers .env.local pour le developpement.
# NE JAMAIS commiter de vraies valeurs dans ce fichier!
# =============================================================================

# -----------------------------------------------------------------------------
# Base de donnees (Neon PostgreSQL)
# -----------------------------------------------------------------------------
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# Obtenez votre connection string depuis: https://console.neon.tech
DATABASE_URL="postgresql://username:password@hostname/database?sslmode=require"

# -----------------------------------------------------------------------------
# Configuration Next.js
# -----------------------------------------------------------------------------
# URL de base de l'application (utilisee pour les liens absolus)
# En production: https://votre-domaine.vercel.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Environnement (development | production | test)
NODE_ENV="development"

# -----------------------------------------------------------------------------
# Securite - OBLIGATOIRE EN PRODUCTION
# -----------------------------------------------------------------------------

# Mot de passe pour le scraping manuel depuis le dashboard
# EXIGENCES:
#   - Minimum 16 caracteres
#   - Melange de majuscules, minuscules, chiffres, symboles
#   - Generer avec: openssl rand -base64 24
SCRAPE_PASSWORD=""

# Secret pour les Vercel Cron jobs
# EXIGENCES:
#   - Generer avec: openssl rand -hex 32
#   - Configurer dans Vercel Dashboard > Settings > Environment Variables
CRON_SECRET=""

# -----------------------------------------------------------------------------
# Optionnel - Rate Limiting (si vous ajoutez Upstash)
# -----------------------------------------------------------------------------
# UPSTASH_REDIS_REST_URL=""
# UPSTASH_REDIS_REST_TOKEN=""

# -----------------------------------------------------------------------------
# Optionnel - Monitoring (si vous ajoutez Sentry)
# -----------------------------------------------------------------------------
# SENTRY_DSN=""
# SENTRY_AUTH_TOKEN=""

# =============================================================================
# CHECKLIST SECURITE AVANT DEPLOIEMENT
# =============================================================================
# [ ] DATABASE_URL configure dans Vercel avec vraie connection string
# [ ] SCRAPE_PASSWORD genere et configure (min 16 chars)
# [ ] CRON_SECRET genere et configure (openssl rand -hex 32)
# [ ] NODE_ENV=production configure dans Vercel
# [ ] Verifier que .env n'est PAS dans le repo git
# [ ] Verifier les logs Vercel pour les erreurs de configuration
# =============================================================================
